// Cree un programa que muestre la tabla de multiplicar del 10, del 1 al 50.


for(let i = 1 ; i <= 50 ; i++) {
    let x = i * 10;

    console.log (`10 * ${i} = ${x}`);
}
console.log(`------------------------`)



for (let i = 0 ; i <= 20; i++) {
    let x = i * 3;

    console.table(`3 * ${i}= ${x}`);
}
