// Cree un programa que muestre los números naturales de 1 a n. Use ciclo while.

let n = parseInt(prompt (`ingrese cantidad de numeros naturales`));
let x = 0;

 while (x < n){
    x++;
    console.log(x);
 }
