/*let p = parseInt(prompt("ingrese las horas trabajadas en la mañana"));

let p2 = parseInt(prompt("ingresa las horas trabajadas en la tarde"));

let v = 8;

let s = "si";

let n = "no";


let h = p + p2 ;


if (h != v) {
    h = h + s ;
    return h;
} else if (h > v){
  h = n;

  return n;
};

  
let $ = 2000;

  let extras = parseInt(prompt("ingresa tus horas extras"));
  let total = $ * extras ;



alert (`
    
    el total del precio de tus horas extras es ${total}`);*/

    /*function myFunction(p1, p2) {
     
      let area = p1 * p2 / 2;

      let valor = `el resultado es ${area}`

      return valor ;
    };

    let total = myFunction(2,3);

    console.log(total);*/


    function rectangulo () {
      let area = base * altura;
      return area ;
    }
    
    let base = parseInt(prompt(`la base es`));

    let altura = parseInt(prompt(`la altura`));

    let total = rectangulo ();

    alert(`la el area es ${total}`)




    