let base = parseInt(prompt("escribe la base del triangulo"));

let altura = parseInt(prompt("eacribe la altura del triangulo"));

let total = base * altura / 2 ;

alert( ` la base del triangulo es ${ base },
    la altura del triangulo es  ${altura},
   el area del triangulo es  ${total} `)