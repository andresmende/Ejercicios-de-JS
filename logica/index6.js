let lado = parseInt(prompt("escribe el lado del cubo"));
//tambien se puede poner lado * lado * laado o si se quiere elvar por 20 seria lado**20
let volumen = lado**3;

alert(`el lado del cubo es ${lado},
    el volumen del cubo es ${volumen}`)