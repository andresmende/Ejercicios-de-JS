let segun = parseInt(prompt( " escribe los segundos "));

let minu = math.floor (segun /60);
 
let  hora = math.floor (minu / 60);

alert (`los segundos son ${segun},
    los minutos son ${minu},
    la hora ${hora}`);